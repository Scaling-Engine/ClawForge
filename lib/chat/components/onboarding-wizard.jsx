'use client';

export default function OnboardingWizard({ initialState, user }) {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <p>Onboarding wizard loading...</p>
    </div>
  );
}
