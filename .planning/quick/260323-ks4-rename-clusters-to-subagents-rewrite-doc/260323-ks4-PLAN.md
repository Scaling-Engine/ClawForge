---
phase: quick
plan: 260323-ks4
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chat/components/app-sidebar.jsx
  - lib/chat/components/admin-layout.jsx
  - lib/chat/components/admin-clusters-page.jsx
  - lib/chat/components/clusters-page.jsx
  - lib/chat/components/cluster-detail-page.jsx
  - lib/chat/components/cluster-console-page.jsx
  - lib/chat/components/cluster-logs-page.jsx
  - lib/chat/components/cluster-role-page.jsx
  - lib/chat/components/cluster-detail-tabs.jsx
  - lib/chat/actions.js
  - docs/OPERATOR_GUIDE.md
  - docs/CONFIGURATION.md
  - docs/DEPLOYMENT.md
  - docs/ARCHITECTURE.md
  - docs/SECURITY.md
  - docs/CHAT_INTEGRATIONS.md
  - docs/VOICE.md
  - docs/CODE_WORKSPACES_V2.md
  - docs/ADMIN_PANEL.md
  - docs/AUTO_MERGE.md
  - docs/CUSTOMIZATION.md
  - docs/SUBAGENTS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Sidebar shows 'Subagents' instead of 'Workflows'"
    - "Admin nav shows 'Subagents' instead of 'Clusters'"
    - "All cluster-related page titles, descriptions, empty states, and button labels say 'Subagents' instead of 'Clusters' or 'Workflows'"
    - "Support page shows user-friendly guide titles (e.g., 'How It Works' not 'Architecture')"
    - "All 11 existing docs rewritten as operator-friendly guides"
    - "New SUBAGENTS.md walkthrough guide exists and appears on support page"
    - "No internal code references (function names, variable names, DB tables, file paths, route URLs) are changed"
  artifacts:
    - path: "docs/SUBAGENTS.md"
      provides: "Subagents walkthrough guide for operators"
  key_links:
    - from: "lib/chat/actions.js"
      to: "docs/SUBAGENTS.md"
      via: "SUPPORT_GUIDES array entry"
      pattern: "slug.*subagents.*SUBAGENTS.md"
---

<objective>
Rename all user-facing "Clusters" / "Workflows" labels to "Subagents" across the UI, rewrite all 11 docs/ files as operator-friendly guides, create a new SUBAGENTS.md walkthrough, and update the support page title mapping.

Purpose: Make the UI terminology consistent ("Subagents") and make documentation accessible to non-technical operators who want to USE the platform, not understand its internals.

Output: Updated .jsx components with "Subagents" labels, 11 rewritten docs, 1 new doc, updated SUPPORT_GUIDES mapping.
</objective>

<execution_context>
@/Users/nwessel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nwessel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

CRITICAL CONSTRAINTS:
- .jsx files are source, .js files are esbuild output (gitignored) — only edit .jsx files for components
- DO NOT rename: function names, variable names, DB table names, file names, route paths/URLs
- DO NOT rename: ClusterIcon import (internal), getClusterConfig, getClusterRuns, etc.
- The /clusters URL path stays unchanged
- Only rename USER-FACING TEXT in JSX content: labels, titles, descriptions, tooltips, button text, empty state messages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all user-facing Clusters/Workflows labels to Subagents</name>
  <files>
    lib/chat/components/app-sidebar.jsx
    lib/chat/components/admin-layout.jsx
    lib/chat/components/admin-clusters-page.jsx
    lib/chat/components/clusters-page.jsx
    lib/chat/components/cluster-detail-page.jsx
    lib/chat/components/cluster-console-page.jsx
    lib/chat/components/cluster-logs-page.jsx
    lib/chat/components/cluster-role-page.jsx
    lib/chat/components/cluster-detail-tabs.jsx
  </files>
  <action>
Read each file and replace ONLY user-facing text strings (JSX text content, not code identifiers):

**app-sidebar.jsx:**
- Line 136-154: Comment "Workflows (formerly Clusters)" → "Subagents"
- Line 148: `<span>Workflows</span>` → `<span>Subagents</span>`
- Line 150: `<TooltipContent side="right">Workflows</TooltipContent>` → `<TooltipContent side="right">Subagents</TooltipContent>`

**admin-layout.jsx:**
- Line 16: ADMIN_NAV entry `label: 'Clusters'` → `label: 'Subagents'`

**admin-clusters-page.jsx:**
- Line 127: `'Add Cluster'` → `'Add Subagent'`, `Edit ${initial.name}` stays (it shows the actual name)
- Line 134: `'Cluster Name *'` → `'Subagent Name *'`
- Line 146: `'Cluster System Prompt (optional)'` → `'Subagent System Prompt (optional)'`
- Line 152: placeholder text `'...all roles in this cluster...'` → `'...all roles in this subagent...'`
- Line 196: Button text `'Add Cluster'` → `'Add Subagent'`
- Line 327: Count text `cluster${...}` → `subagent${...}`
- Line 335: Button `'Add Cluster'` → `'Add Subagent'`
- Line 357: Empty state heading `'No clusters configured'` → `'No subagents configured'`
- Line 359: Description `'Clusters define multi-agent teams...'` → `'Subagents are multi-agent teams. Add a subagent with roles to enable parallel agent dispatch.'`
- Line 364: Button `'Add First Cluster'` → `'Add First Subagent'`

**clusters-page.jsx:**
- Line 180: Empty state text `'No clusters configured'` → `'No subagents configured'`
- Line 182: Help text about CLUSTER.json stays (it's a real filename)
- Line 314: Empty runs text `'No cluster runs yet.'` → `'No subagent runs yet.'`
- Line 355: Page title `'Workflows'` → `'Subagents'`
- Line 357: Description `'Multi-agent workflows — chain multiple agents...'` → `'Multi-agent subagents — chain multiple agents together to tackle complex tasks in sequence.'`
- Line 364: Section heading `'Cluster Definitions'` → `'Subagent Definitions'`

**cluster-detail-page.jsx:** Change any user-facing page title/breadcrumb text from "Cluster" to "Subagent". Keep all code references unchanged.

**cluster-console-page.jsx:** Change any user-facing page title from "Cluster Console" to "Subagent Console". Keep code references unchanged.

**cluster-logs-page.jsx:** Change any user-facing page title from "Cluster Logs" to "Subagent Logs". Keep code references unchanged.

**cluster-role-page.jsx:** Change any user-facing breadcrumb/title text from "Cluster" to "Subagent". Keep code references unchanged.

**cluster-detail-tabs.jsx:** Tab labels (Overview, Console, Logs) are generic and do NOT need changing. Only change if there are any "Cluster" strings in user-facing labels.

DO NOT change: function/component names (ClustersPage, ClusterCard, etc.), import paths, ClusterIcon, variable names, href paths (/clusters/...), action function names (getClusterConfig, etc.).
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All user-facing text in 9 component files says "Subagent(s)" instead of "Cluster(s)" or "Workflow(s)". No code identifiers, file paths, or route URLs changed. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite 11 docs as operator guides, create SUBAGENTS.md, update support mapping</name>
  <files>
    docs/OPERATOR_GUIDE.md
    docs/CONFIGURATION.md
    docs/DEPLOYMENT.md
    docs/ARCHITECTURE.md
    docs/SECURITY.md
    docs/CHAT_INTEGRATIONS.md
    docs/VOICE.md
    docs/CODE_WORKSPACES_V2.md
    docs/ADMIN_PANEL.md
    docs/AUTO_MERGE.md
    docs/CUSTOMIZATION.md
    docs/SUBAGENTS.md
    lib/chat/actions.js
  </files>
  <action>
**IMPORTANT:** Read each existing doc FIRST before rewriting. Preserve all factual content — just reframe for operators.

**Rewrite guidelines for ALL 11 docs:**
- Start with a clear H1 title using the friendly name (see mapping below)
- Second line: brief intro paragraph explaining what this guide covers and who it's for
- Use "you/your" language, not "the system" or "the architecture"
- Replace jargon with plain language where possible
- Use step-by-step instructions where applicable
- Keep code blocks/commands but explain what they do
- Remove internal implementation details operators don't need
- Add a "Quick Start" or "TL;DR" section at top where appropriate
- Use the term "Subagents" (not Clusters/Workflows) when referring to multi-agent features

**Title mapping (H1 in doc and SUPPORT_GUIDES title):**
1. `OPERATOR_GUIDE.md` → "Getting Started" (title: "Getting Started")
2. `CONFIGURATION.md` → "Settings & Configuration" (title: "Settings & Configuration")
3. `DEPLOYMENT.md` → "Deploying Your Instance" (title: "Deploying Your Instance")
4. `ARCHITECTURE.md` → "How It Works" (title: "How It Works")
5. `SECURITY.md` → "Keeping Your Instance Safe" (title: "Keeping Your Instance Safe")
6. `CHAT_INTEGRATIONS.md` → "Connecting Slack & Telegram" (title: "Connecting Slack & Telegram")
7. `VOICE.md` → "Using Voice Input" (title: "Using Voice Input")
8. `CODE_WORKSPACES_V2.md` → "Interactive Code Sessions" (title: "Interactive Code Sessions")
9. `ADMIN_PANEL.md` → "Admin Settings Guide" (title: "Admin Settings Guide")
10. `AUTO_MERGE.md` → "Auto-Merge Rules" (title: "Auto-Merge Rules")
11. `CUSTOMIZATION.md` → "Customizing Your Agent" (title: "Customizing Your Agent")

**Create NEW file `docs/SUBAGENTS.md`:**
- Title: "Using Subagents"
- Explain what subagents are in plain language (multi-agent workflows that chain agents together)
- How to create one: via Admin > Subagents page, define roles with names + system prompts + allowed tools
- How to run one: triggered by the system or manually
- What to expect: each role runs as a separate job, results combine
- Reference the CLUSTER.json config file format
- Keep it practical — operators want to know HOW, not the internals

**Update `lib/chat/actions.js`:**
Update the SUPPORT_GUIDES array (around line 1424-1436) to:
1. Use the new friendly titles from the mapping above
2. Add a new entry for SUBAGENTS.md:
   `{ slug: 'subagents', title: 'Using Subagents', filename: 'SUBAGENTS.md' }`

Place the subagents entry after the customization entry (last in the list).
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && node -e "const fs = require('fs'); const guides = ['OPERATOR_GUIDE.md','CONFIGURATION.md','DEPLOYMENT.md','ARCHITECTURE.md','SECURITY.md','CHAT_INTEGRATIONS.md','VOICE.md','CODE_WORKSPACES_V2.md','ADMIN_PANEL.md','AUTO_MERGE.md','CUSTOMIZATION.md','SUBAGENTS.md']; let ok=true; for(const g of guides){const p='docs/'+g; if(!fs.existsSync(p)){console.log('MISSING:',p);ok=false}else{const c=fs.readFileSync(p,'utf8'); if(c.length<200){console.log('TOO SHORT:',p,c.length);ok=false}}} if(ok)console.log('All 12 docs exist and have content'); const actions=fs.readFileSync('lib/chat/actions.js','utf8'); if(!actions.includes('SUBAGENTS.md')){console.log('FAIL: SUBAGENTS.md not in SUPPORT_GUIDES');ok=false} if(!actions.includes('Using Subagents')){console.log('FAIL: title not updated');ok=false} process.exit(ok?0:1)"</automated>
  </verify>
  <done>All 11 docs rewritten with operator-friendly language and new titles. SUBAGENTS.md created as walkthrough guide. SUPPORT_GUIDES array in actions.js updated with new titles and includes SUBAGENTS.md entry. No docs are empty or stub-only.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds — no broken imports or JSX errors
2. All 12 docs exist in docs/ with meaningful content (>200 chars each)
3. SUPPORT_GUIDES in actions.js has 12 entries with friendly titles
4. Grep for user-facing "Clusters" or "Workflows" in the 9 .jsx files returns zero hits (excluding code comments about history like "formerly Clusters")
5. Grep for code-level renames (function names, imports, routes) shows zero changes
</verification>

<success_criteria>
- Every user-facing label in the 9 component files says "Subagent(s)" — zero instances of "Cluster(s)" or "Workflow(s)" in rendered text
- All 12 docs/\*.md files (11 rewritten + 1 new) are operator-friendly guides with clear intros
- SUPPORT_GUIDES array has 12 entries with friendly titles
- Build succeeds
- No internal code identifiers, file paths, or route URLs were changed
</success_criteria>

<output>
After completion, create `.planning/quick/260323-ks4-rename-clusters-to-subagents-rewrite-doc/260323-ks4-SUMMARY.md`
</output>
