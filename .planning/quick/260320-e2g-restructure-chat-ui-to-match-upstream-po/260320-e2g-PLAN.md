---
phase: quick-260320-e2g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chat/components/chat-header.jsx
  - lib/chat/components/chat-input.jsx
  - lib/chat/components/chat.jsx
  - lib/chat/components/greeting.jsx
autonomous: false
requirements: [UI-RESTRUCTURE]
must_haves:
  truths:
    - "Header shows only agent name and sidebar trigger — no repo/branch selectors"
    - "Code toggle is a pill-style switch below the chat input, not a </> button inside the input bar"
    - "Repo/branch selectors appear below the chat input only when Code toggle is ON"
    - "Plan/Code sub-mode dropdown is completely removed"
    - "Interactive button is replaced with a Headless toggle below the input"
    - "Greeting says 'What we coding today?' when Code mode is active"
  artifacts:
    - path: "lib/chat/components/chat-header.jsx"
      provides: "Clean header with agent name + sidebar trigger only"
    - path: "lib/chat/components/chat-input.jsx"
      provides: "Clean input bar — paperclip, mic, textarea, send only"
    - path: "lib/chat/components/chat.jsx"
      provides: "Below-input control bar with Code toggle, repo/branch, Headless"
    - path: "lib/chat/components/greeting.jsx"
      provides: "Context-aware greeting based on codeActive prop"
  key_links:
    - from: "lib/chat/components/chat.jsx"
      to: "lib/chat/components/greeting.jsx"
      via: "codeActive prop"
      pattern: "codeActive.*Greeting"
    - from: "lib/chat/components/chat.jsx"
      to: "lib/chat/components/chat-input.jsx"
      via: "cleaned props (no code controls)"
      pattern: "ChatInput"
---

<objective>
Restructure the chat UI to match the upstream PopeBot layout. Move Code toggle and repo/branch selectors out of the header and input bar into a dedicated control bar below the chat input. Replace the Interactive button with a Headless toggle. Remove the Plan/Code sub-mode dropdown entirely. Clean the header to agent name + sidebar trigger only.

Purpose: Match upstream PopeBot visual layout and interaction patterns for consistency.
Output: Four modified .jsx component files with restructured UI controls.
</objective>

<execution_context>
@/Users/nwessel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nwessel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/chat/components/chat.jsx
@lib/chat/components/chat-input.jsx
@lib/chat/components/chat-header.jsx
@lib/chat/components/greeting.jsx
@lib/chat/components/code/actions.js

<interfaces>
<!-- Current component contracts that will change -->

From lib/chat/components/chat-input.jsx (line 45):
```jsx
export function ChatInput({ input, setInput, onSubmit, status, stop, files, setFiles, codeActive = false, onToggleCode, codeSubMode = 'plan', onChangeCodeSubMode, onLaunchInteractive, isLaunching = false, linkedWorkspaceId = null, hasRepoSelected = false })
```

From lib/chat/components/greeting.jsx (line 6):
```jsx
export function Greeting()
// No props currently — needs codeActive prop added
```

From lib/chat/components/chat-header.jsx (line 8):
```jsx
export function ChatHeader({ chatId })
// Currently fetches repos/branches — those move to chat.jsx control bar
```

From lib/chat/repo-chat-context.js (used by chat.jsx):
```jsx
const { selectedRepo, setSelectedRepo, selectedBranch, setSelectedBranch } = useRepoChat();
```

From lib/chat/actions.js (used by header, needed in chat.jsx for repo/branch):
```jsx
export async function getRepos()
export async function getBranches(owner, slug)
export async function getAgentName()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip header and input bar, add below-input control bar in chat.jsx</name>
  <files>lib/chat/components/chat-header.jsx, lib/chat/components/chat-input.jsx, lib/chat/components/chat.jsx</files>
  <action>
**chat-header.jsx** — Remove ALL repo/branch selector logic. Keep only:
- SidebarTrigger (mobile)
- Agent name span (desktop)
- The useEffect for getAgentName
- Remove: useState for repos/branches/loadingBranches, useRef for branchLoadingForRepo, handleRepoChange, handleBranchChange, the select elements, the useRepoChat import, getRepos/getBranches imports.

**chat-input.jsx** — Remove Code toggle button, Plan/Code sub-mode dropdown, and Interactive button from inside the input bar. Specifically:
- Remove the `{onToggleCode && (...)}` block (lines 273-290) — the `</>` button
- Remove the `{codeActive && onToggleCode && (...)}` select for sub-mode (lines 293-304)
- Remove the `{codeActive && onToggleCode && (...)}` Interactive button (lines 307-324)
- Remove props from function signature: `onToggleCode`, `codeSubMode`, `onChangeCodeSubMode`, `onLaunchInteractive`, `isLaunching`, `linkedWorkspaceId`, `hasRepoSelected`
- Keep: `codeActive` prop (still used for font-mono on textarea)
- Keep: paperclip, mic/voice, textarea, send/stop — unchanged

**chat.jsx** — This is the main orchestration change:
1. Remove `codeSubMode` state and `setCodeSubMode` entirely.
2. Remove `codeSubMode` from the transport body (or hardcode to undefined since sub-mode is gone).
3. Remove `codeSubMode` from the useMemo dependency array.
4. Add repo/branch fetching logic (move from chat-header.jsx): import `getRepos`, `getBranches` from actions.js. Add states: `repos`, `branches`, `loadingBranches`. Add useEffect to fetch repos on mount. Add `handleRepoChange(slug)` and `handleBranchChange(branch)` functions. These use `setSelectedRepo`/`setSelectedBranch` from useRepoChat context.
5. Create a new `BelowInputBar` section rendered AFTER each `<ChatInput>` usage (both in the empty-messages greeting view and the messages view). This bar renders:

   a. **Code toggle** (pill-style) — Only visible when `canUseCode` is true. An iOS-style toggle switch: a 44x24px rounded-full container with a 20x20 circle that slides left/right. When OFF: gray background, circle on left. When ON: accent/green background, circle on right. Label "Code" next to it. Uses `codeActive` state + `setCodeActive` toggle. CSS transition for smooth slide.

   b. **Repo selector** — Only visible when `codeActive` is true. Same select element currently in chat-header.jsx, styled to match: small text, border, rounded. Shows "No repo selected" default.

   c. **Branch selector** — Only visible when `codeActive && selectedRepo` is true. Same select as header had, with loading state.

   d. **Headless toggle** — Only visible when `codeActive` is true. A toggle switch (same pill style as Code toggle but smaller or same size). Label "Headless". When toggled ON, calls `handleLaunchInteractive` (same logic — launches workspace or resumes). When already linked to a workspace, shows as ON with "Resume" or navigates on click. Disabled when no repo selected.

6. Layout: The BelowInputBar is a flex row, centered, with gap-3, rendered inside the same max-w-4xl container as ChatInput. Items: Code toggle | repo select | branch select | Headless toggle. Wrap on mobile (flex-wrap).

7. Remove from ChatInput props: `onToggleCode`, `codeSubMode`, `onChangeCodeSubMode`, `onLaunchInteractive`, `isLaunching`, `linkedWorkspaceId`, `hasRepoSelected`. Keep passing `codeActive`.
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Header shows only agent name + sidebar trigger. Input bar contains only paperclip, mic, textarea, send/stop. Below-input control bar shows Code pill toggle, and when Code ON shows repo/branch selectors and Headless toggle. No Plan/Code dropdown exists anywhere. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update Greeting to accept codeActive prop</name>
  <files>lib/chat/components/greeting.jsx, lib/chat/components/chat.jsx</files>
  <action>
**greeting.jsx** — Add `codeActive` prop to the Greeting component:
```jsx
export function Greeting({ codeActive = false })
```
When `codeActive` is true, show a different greeting: "What we coding today?" (no agent name prefix needed). When false, keep existing behavior: "Hello! I'm {agentName}. How can I help?" or "Hello! How can I help?".

The codeActive greeting should use the same text styling (font-semibold text-2xl md:text-3xl text-foreground) but can optionally add font-mono class for the coding greeting.

**chat.jsx** — Pass `codeActive` to both Greeting usages:
```jsx
<Greeting codeActive={codeActive} />
```
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Greeting shows "What we coding today?" when Code toggle is ON, standard greeting when OFF. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify restructured chat UI visually</name>
  <files>lib/chat/components/chat.jsx</files>
  <action>Visual verification of the restructured chat UI matching upstream PopeBot layout.</action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All UI changes verified by user — header clean, controls below input, greeting context-aware.</done>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. Verify header shows only agent name + sidebar trigger (no repo/branch dropdowns)
    3. Verify chat input bar contains only: paperclip, mic (if supported), textarea, send button
    4. Below the input, verify "Code" pill toggle is visible (admin user only)
    5. Toggle Code ON — verify repo/branch selectors and Headless toggle appear below input
    6. Toggle Code ON — verify greeting changes to "What we coding today?"
    7. Toggle Code OFF — verify repo/branch and Headless disappear, greeting returns to normal
    8. Verify NO Plan/Code sub-mode dropdown exists anywhere
    9. Verify NO Interactive button exists inside the input bar
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- No references to `codeSubMode` remain in any of the four .jsx files
- No `onChangeCodeSubMode` or `onLaunchInteractive` props passed to ChatInput
- Header component has no repo/branch select elements
- Greeting component accepts and uses `codeActive` prop
</verification>

<success_criteria>
- Chat header is clean: agent name + sidebar trigger only
- Code toggle is a pill-style switch below the chat input
- Repo/branch selectors appear below input only when Code is ON
- Plan/Code sub-mode dropdown is completely gone
- Interactive button replaced by Headless toggle below input
- Greeting changes when Code mode is active
- Build passes, no regressions
</success_criteria>

<output>
After completion, create `.planning/quick/260320-e2g-restructure-chat-ui-to-match-upstream-po/260320-e2g-SUMMARY.md`
</output>
