---
phase: 16-pr-pipeline-auto-merge-exclusion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/auto-merge.yml
  - templates/.github/workflows/auto-merge.yml
  - templates/docker/job/entrypoint.sh
autonomous: true
requirements: [DELIV-01, DELIV-02]

must_haves:
  truths:
    - "Instance scaffolding PRs (touching instances/ or docker-compose.yml) are never auto-merged, even when ALLOWED_PATHS=/"
    - "Regular job PRs that only touch allowed paths still auto-merge normally"
    - "PR body uses --body-file when /tmp/pr-body.md exists, avoiding shell argument length limits"
    - "Both auto-merge.yml copies (.github/workflows/ and templates/.github/workflows/) are identical"
  artifacts:
    - path: ".github/workflows/auto-merge.yml"
      provides: "Blocked-paths step before ALLOWED_PATHS check"
      contains: "instances/"
    - path: "templates/.github/workflows/auto-merge.yml"
      provides: "Template copy of auto-merge workflow (identical to .github copy)"
      contains: "instances/"
    - path: "templates/docker/job/entrypoint.sh"
      provides: "PR creation with --body-file flag"
      contains: "--body-file"
  key_links:
    - from: ".github/workflows/auto-merge.yml"
      to: "Merge PR step"
      via: "check-blocked output gates merge step"
      pattern: "steps\\.check-blocked\\.outputs\\.blocked != 'true'"
---

<objective>
Add explicit blocked-paths defense to auto-merge workflow so instance scaffolding PRs never auto-merge, and switch entrypoint.sh to --body-file for robust PR body delivery.

Purpose: Instance PRs modify infrastructure-critical files (docker-compose.yml, instance configs). They must always require human review, regardless of ALLOWED_PATHS configuration. The --body-file switch prevents shell argument length issues with long operator setup checklists.

Output: Updated auto-merge.yml (both copies) with blocked-paths step, updated entrypoint.sh with --body-file convention.
</objective>

<execution_context>
@/Users/nwessel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nwessel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-pr-pipeline-auto-merge-exclusion/16-RESEARCH.md

<interfaces>
<!-- Current auto-merge.yml step structure (both copies are identical): -->
<!-- Step 1: "Wait for merge check" (id: merge-check) → outputs.mergeable -->
<!-- Step 2: "Check AUTO_MERGE setting" (id: check-setting) → outputs.enabled -->
<!-- Step 3: "Check ALLOWED_PATHS" (id: check-paths) → outputs.allowed -->
<!-- Step 4: "Merge PR" — conditioned on all 3 outputs -->

<!-- Entrypoint.sh PR creation (lines 268-276): -->
<!-- Uses `--body "$PR_BODY"` with fallback to "Automated job by ClawForge" -->
<!-- Phase 15's buildValidationChecklist() instructs container agent to write /tmp/pr-body.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add blocked-paths step to auto-merge workflow</name>
  <files>.github/workflows/auto-merge.yml, templates/.github/workflows/auto-merge.yml</files>
  <action>
Insert a new step "Check for blocked paths (instance scaffolding)" between the "Check AUTO_MERGE setting" step and the "Check ALLOWED_PATHS" step in `.github/workflows/auto-merge.yml`.

The new step (id: check-blocked):
- Condition: `steps.merge-check.outputs.mergeable == 'MERGEABLE' && steps.check-setting.outputs.enabled == 'true'`
- Gets changed files via `gh pr diff "$PR_NUMBER" --name-only --repo "${{ github.repository }}"`
- Iterates each file: if it matches `instances/*` or equals `docker-compose.yml`, sets `BLOCKED=true` and logs the blocked file
- Outputs `blocked=$BLOCKED` via GITHUB_OUTPUT
- Uses `GH_TOKEN: ${{ github.token }}` env

Update the "Check ALLOWED_PATHS" step condition to also require `steps.check-blocked.outputs.blocked != 'true'`.

Update the "Merge PR" step condition to also require `steps.check-blocked.outputs.blocked != 'true'`.

The final Merge PR condition should be:
`steps.merge-check.outputs.mergeable == 'MERGEABLE' && steps.check-setting.outputs.enabled == 'true' && steps.check-blocked.outputs.blocked != 'true' && steps.check-paths.outputs.allowed == 'true'`

After editing `.github/workflows/auto-merge.yml`, copy it exactly to `templates/.github/workflows/auto-merge.yml` so both files are identical.
  </action>
  <verify>
    <automated>diff /Users/nwessel/Claude\ Code/Business/Products/clawforge/.github/workflows/auto-merge.yml /Users/nwessel/Claude\ Code/Business/Products/clawforge/templates/.github/workflows/auto-merge.yml && grep -c "instances/" /Users/nwessel/Claude\ Code/Business/Products/clawforge/.github/workflows/auto-merge.yml && grep -c "check-blocked" /Users/nwessel/Claude\ Code/Business/Products/clawforge/.github/workflows/auto-merge.yml</automated>
  </verify>
  <done>Both auto-merge.yml files contain a blocked-paths step that prevents merging PRs touching instances/ or docker-compose.yml. Both files are identical. Existing merge conditions preserved and extended.</done>
</task>

<task type="auto">
  <name>Task 2: Switch entrypoint.sh to --body-file for PR creation</name>
  <files>templates/docker/job/entrypoint.sh</files>
  <action>
Replace lines 268-276 in `templates/docker/job/entrypoint.sh`. The current code reads /tmp/pr-body.md into a shell variable and passes it via `--body "$PR_BODY"`. Replace with a branching pattern:

If `/tmp/pr-body.md` exists, use `--body-file /tmp/pr-body.md` directly (avoids shell argument length limits for long PR bodies like instance setup checklists).

If `/tmp/pr-body.md` does not exist, use `--body "Automated job by ClawForge"` as the fallback.

Both branches create the PR with `--title "clawforge: job ${JOB_ID}" --base main || true`.

The replacement block:
```bash
# Create PR only if Claude succeeded AND produced commits
if [ "$CLAUDE_EXIT" -eq 0 ] && [ "$HAS_NEW_COMMIT" = "true" ]; then
    if [ -f /tmp/pr-body.md ]; then
        gh pr create \
            --title "clawforge: job ${JOB_ID}" \
            --body-file /tmp/pr-body.md \
            --base main || true
    else
        gh pr create \
            --title "clawforge: job ${JOB_ID}" \
            --body "Automated job by ClawForge" \
            --base main || true
    fi
else
    echo "Skipping PR: CLAUDE_EXIT=${CLAUDE_EXIT}, HAS_NEW_COMMIT=${HAS_NEW_COMMIT}"
fi
```

Do NOT change any other lines in entrypoint.sh. The logic flow (check CLAUDE_EXIT and HAS_NEW_COMMIT) must remain identical.
  </action>
  <verify>
    <automated>grep -c "body-file" /Users/nwessel/Claude\ Code/Business/Products/clawforge/templates/docker/job/entrypoint.sh && grep -c "pr-body.md" /Users/nwessel/Claude\ Code/Business/Products/clawforge/templates/docker/job/entrypoint.sh</automated>
  </verify>
  <done>entrypoint.sh uses --body-file when /tmp/pr-body.md exists, falls back to --body for default message. No shell variable expansion of PR body content.</done>
</task>

</tasks>

<verification>
1. `diff .github/workflows/auto-merge.yml templates/.github/workflows/auto-merge.yml` — no differences (files in sync)
2. `grep "instances/" .github/workflows/auto-merge.yml` — blocked-paths step exists
3. `grep "docker-compose.yml" .github/workflows/auto-merge.yml` — docker-compose also blocked
4. `grep "check-blocked" .github/workflows/auto-merge.yml` — step ID present and referenced in conditions
5. `grep "body-file" templates/docker/job/entrypoint.sh` — --body-file flag used
6. `npm test` — no regressions in existing test suite
</verification>

<success_criteria>
- auto-merge.yml has a blocked-paths step that prevents merging PRs touching instances/ or docker-compose.yml, independent of ALLOWED_PATHS
- Both copies of auto-merge.yml are identical
- entrypoint.sh uses --body-file /tmp/pr-body.md when available
- Existing auto-merge behavior for regular job PRs is unchanged
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-pr-pipeline-auto-merge-exclusion/16-01-SUMMARY.md`
</output>
