---
phase: 15-job-prompt-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/tools/instance-job.js
  - lib/ai/tools.js
  - tests/test-instance-job.js
autonomous: true
requirements: [SCAF-01, SCAF-02, SCAF-03, SCAF-04]

must_haves:
  truths:
    - "buildInstanceJobDescription() returns a prompt string containing all 6 instance file paths"
    - "The prompt embeds literal file templates with substitution values filled in from config"
    - "AGENT.md section contains exact tool casing: Read,Write,Edit,Bash,Glob,Grep,Task,Skill"
    - "SOUL.md section reflects the operator's stated purpose, not generic boilerplate"
    - "REPOS.json section is scoped to only the config's allowed_repos with owner defaulting to ScalingEngine"
    - "EVENT_HANDLER.md section only lists enabled channels from config"
    - "docker-compose.yml modification instructions use targeted Edit insertions, not full-file rewrite"
    - ".env.example section only includes channel-specific vars for enabled channels"
  artifacts:
    - path: "lib/tools/instance-job.js"
      provides: "buildInstanceJobDescription(config) function"
      exports: ["buildInstanceJobDescription"]
    - path: "tests/test-instance-job.js"
      provides: "Unit tests for prompt output correctness"
      min_lines: 80
    - path: "lib/ai/tools.js"
      provides: "Updated createInstanceJobTool handler calling buildInstanceJobDescription"
  key_links:
    - from: "lib/ai/tools.js"
      to: "lib/tools/instance-job.js"
      via: "import { buildInstanceJobDescription }"
      pattern: "import.*buildInstanceJobDescription.*from.*instance-job"
    - from: "lib/ai/tools.js"
      to: "lib/tools/create-job.js"
      via: "createJob(description) call with new prompt"
      pattern: "createJob\\(description\\)"
---

<objective>
Create `buildInstanceJobDescription(config)` that produces a complete, self-contained job prompt causing the Claude Code container agent to generate all 7 instance artifacts with semantically valid, purpose-scoped content. Wire it into the existing `createInstanceJobTool` handler replacing the Phase 13 stub.

Purpose: This is the core of the instance generator -- the prompt IS the product. Every file the container agent must produce, every constraint it must respect, and every validation it must perform is encoded in this single function's output.

Output: `lib/tools/instance-job.js` (function), updated `lib/ai/tools.js` (wiring), `tests/test-instance-job.js` (unit tests)
</objective>

<execution_context>
@/Users/nwessel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nwessel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-job-prompt-completeness/15-RESEARCH.md

@lib/ai/tools.js
@lib/tools/create-job.js
@instances/noah/Dockerfile
@instances/noah/config/SOUL.md
@instances/noah/config/AGENT.md
@instances/noah/config/EVENT_HANDLER.md
@instances/noah/config/REPOS.json
@instances/noah/.env.example
@instances/strategyES/config/SOUL.md
@instances/strategyES/config/AGENT.md
@instances/strategyES/config/EVENT_HANDLER.md
@instances/strategyES/config/REPOS.json
@docker-compose.yml

<interfaces>
<!-- From lib/ai/tools.js lines 143-185: current stub to replace -->
<!-- The handler receives these params from Zod schema: -->
<!--   name: string (instance slug, lowercase, no spaces) -->
<!--   purpose: string (what instance is for) -->
<!--   allowed_repos: string[] (GitHub repo slugs) -->
<!--   enabled_channels: ('slack'|'telegram'|'web')[] -->
<!--   slack_user_ids?: string[] -->
<!--   telegram_chat_id?: string -->

<!-- From lib/tools/create-job.js: -->
<!--   createJob(description: string) -> { job_id, branch } -->
<!--   description becomes the contents of job.md -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create buildInstanceJobDescription() with unit tests</name>
  <files>lib/tools/instance-job.js, tests/test-instance-job.js</files>
  <behavior>
    - Test: Given config {name:"testbot", purpose:"Test dev agent", allowed_repos:["my-repo"], enabled_channels:["slack"]}, output contains "instances/testbot/Dockerfile"
    - Test: Given config with allowed_repos:["my-repo"], output contains REPOS.json block with "owner": "ScalingEngine" and "slug": "my-repo"
    - Test: Output always contains exact string "Read,Write,Edit,Bash,Glob,Grep,Task,Skill" (AGENT.md tool casing)
    - Test: Given config with purpose "Marketing automation agent for Acme Corp", SOUL.md section contains "Marketing automation" and "Acme Corp" (not "Noah" or "Archie")
    - Test: Given enabled_channels:["slack"], output does NOT contain "TELEGRAM_BOT_TOKEN" or "TELEGRAM_WEBHOOK_SECRET" in env example section
    - Test: Given enabled_channels:["slack","telegram","web"], output contains all three channel env var blocks
    - Test: Output contains docker-compose modification instructions mentioning Edit tool, NOT Write tool for docker-compose.yml
    - Test: Output contains "instances/testbot/config/SOUL.md", "instances/testbot/config/AGENT.md", "instances/testbot/config/EVENT_HANDLER.md", "instances/testbot/config/REPOS.json", "instances/testbot/.env.example" (all 6 files)
    - Test: SOUL.md section does NOT contain literal "$" or backtick characters outside fenced code blocks
    - Test: Given enabled_channels:["web"], EVENT_HANDLER.md section does NOT mention "Slack" or "Telegram" as available channels
  </behavior>
  <action>
Create `lib/tools/instance-job.js` exporting `buildInstanceJobDescription(config)`.

The function builds a prompt string using template literals (NO template engines -- ESM project, all template engines are CommonJS). Structure:

**1. Config block** -- JSON-stringified config at top with derived values:
- `env_prefix`: `config.name.toUpperCase().replace(/-/g, '_')`
- `allowed_repos` mapped to full REPOS.json schema objects: each gets `{ owner: "ScalingEngine", slug, name: slugToDisplayName(slug), aliases: [slug] }`
- `primary_repo`: first repo in list (used for GH_REPO default)

**2. File manifest** -- List all 7 artifacts the agent must produce.

**3. Literal templates with filled values** -- For each of the 6 instance files, embed the COMPLETE file content with config values already substituted in. Do NOT use {{placeholders}} that the container LLM must replace -- do the substitution in JavaScript so the prompt contains the exact final file content the agent should write.

The templates (derive from reading the actual noah and strategyES instances):

- **Dockerfile**: Copy noah's Dockerfile verbatim, replacing only the 4 `instances/noah/config/` paths with `instances/{config.name}/config/` and updating the header comment.

- **SOUL.md**: Generate purpose-scoped content. Structure: persona heading (derive a persona name from purpose or use instance name), identity fields (name, scope, owner derived from purpose), communication style (derived from purpose), restrictions (if repos are limited, state scope restriction). CRITICAL: Must NOT contain `$` or backtick characters. Use the strategyES SOUL.md as the structural template but fill with purpose-derived content.

- **AGENT.md**: Start with strategyES AGENT.md as template (it has the Scope section). Replace:
  - "What You Are" paragraph: scope to config's repos
  - "Scope" section: restrict to allowed_repos
  - "Working Directory" section: reference primary repo
  - "Tech Stack" section: omit (cannot infer stack from purpose alone)
  - Everything from "Available Tools" onward: copy VERBATIM from noah's AGENT.md (identical across instances). The tools list MUST read exactly: `Read`, `Write`, `Edit`, `Bash`, `Glob`, `Grep`, `Task`, `Skill`.

- **EVENT_HANDLER.md**: Build from noah's EVENT_HANDLER.md structure but:
  - "Your Role" section: scope to config's purpose and enabled channels only
  - "Scope Restrictions" section: add if repos are limited (copy pattern from strategyES)
  - "Available Repositories" section: list only config's allowed_repos
  - Remove "Available MCP Servers & Integrations" section (instance-specific, cannot infer)
  - "GSD Workflow" through "Checking Job Status" sections: copy VERBATIM (shared boilerplate)
  - "Conversational Guidance" and "Examples": adapt to purpose
  - "Instance Creation Intake" section: OMIT (only noah's instance gets this)
  - Channel references: only mention enabled_channels (e.g., if only slack, do not mention Telegram or Web Chat)

- **REPOS.json**: Build from config.allowed_repos array. Each repo gets: `{ owner: "ScalingEngine", slug: repoSlug, name: deriveDisplayName(repoSlug), aliases: [repoSlug, ...shortAliases] }`. Helper `deriveDisplayName` converts slug to title case (e.g., "strategyes-lab" -> "StrategyES Lab" -- but use a simple capitalize-words approach, exact casing is not critical).

- **.env.example**: Base vars always present (APP_URL, APP_HOSTNAME, AUTH_SECRET, GH_TOKEN, GH_OWNER, GH_REPO, GH_WEBHOOK_SECRET, LLM_PROVIDER, LLM_MODEL, ANTHROPIC_API_KEY). Then conditionally include channel blocks:
  - If "slack" in enabled_channels: SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, SLACK_ALLOWED_USERS, SLACK_ALLOWED_CHANNELS, OPENAI_API_KEY (for Whisper)
  - If "telegram" in enabled_channels: TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, TELEGRAM_CHAT_ID
  - If "web" in enabled_channels: AUTH_TRUST_HOST=true

**4. docker-compose.yml modification instructions** -- Instruct the container agent to make 4 targeted edits using the Edit tool (NOT Write):
  1. Add `- {name}-net` to traefik's networks list
  2. Insert new service block after last existing service (before `volumes:` section). Service name: `{name}-event-handler`, container_name: `clawforge-{name}`, network: `{name}-net` + `proxy-net`, env vars with `{ENV_PREFIX}_` prefix, volumes: `{name}-data` + `{name}-config`, traefik labels with `{name}` router prefix, healthcheck, restart policy. Only include channel-specific env vars for enabled channels.
  3. Add `{name}-net: driver: bridge` to networks section
  4. Add `{name}-data:` and `{name}-config:` to volumes section

Provide the exact content for each edit so the agent can use the Edit tool directly.

**5. Validation checklist** -- Tell the agent to verify before committing:
  - All 6 files exist under `instances/{name}/`
  - docker-compose.yml has the new service, network, volumes, and traefik network entry
  - AGENT.md contains exact tool casing
  - SOUL.md has no `$` or backtick chars
  - REPOS.json is valid JSON
  - .env.example has correct channel-conditional vars

Then write the test file `tests/test-instance-job.js` using Node.js built-in test runner (`node:test`). Import `buildInstanceJobDescription` and test all behaviors listed above. Tests call the function with different configs and assert on the output string using `includes()`, `match()`, and negative assertions.
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && node --test tests/test-instance-job.js</automated>
  </verify>
  <done>
    - buildInstanceJobDescription() exported from lib/tools/instance-job.js
    - Given any valid config, output contains all 6 instance file paths
    - AGENT.md section has exact tool casing Read,Write,Edit,Bash,Glob,Grep,Task,Skill
    - SOUL.md section reflects purpose, has no shell-unsafe chars
    - REPOS.json section scoped to config repos with ScalingEngine owner
    - EVENT_HANDLER.md section scoped to enabled channels only
    - docker-compose instructions use Edit tool, not Write
    - .env.example conditional on enabled channels
    - All unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire buildInstanceJobDescription into createInstanceJobTool</name>
  <files>lib/ai/tools.js</files>
  <action>
Replace the Phase 13 stub in `lib/ai/tools.js` (lines 148-158, the `description` construction) with a call to `buildInstanceJobDescription()`.

1. Add import at top of file: `import { buildInstanceJobDescription } from '../tools/instance-job.js';`

2. Replace lines 148-158 (the `const description = [...]` block) with:
```javascript
const description = buildInstanceJobDescription({
  name,
  purpose,
  allowed_repos,
  enabled_channels,
  slack_user_ids,
  telegram_chat_id,
});
```

3. The rest of the handler (createJob call, saveJobOrigin, return) stays identical.

4. Verify the import resolves correctly and the existing tool export array at line 187 is unchanged.
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && node -e "import('./lib/ai/tools.js').then(() => console.log('OK')).catch(e => { console.error(e.message); process.exit(1); })"</automated>
  </verify>
  <done>
    - lib/ai/tools.js imports buildInstanceJobDescription from lib/tools/instance-job.js
    - createInstanceJobTool handler passes config object to buildInstanceJobDescription
    - No other changes to tools.js (exports, other tools unchanged)
    - Module resolves without import errors
  </done>
</task>

</tasks>

<verification>
1. `node --test tests/test-instance-job.js` -- all unit tests pass
2. `node -e "import('./lib/ai/tools.js').then(() => console.log('OK'))"` -- tools module loads without errors
3. `npm test` -- full test suite passes (no regressions)
</verification>

<success_criteria>
- buildInstanceJobDescription(config) produces a prompt that references all 7 artifacts (6 files + docker-compose update)
- Unit tests verify: tool casing, purpose-scoping, channel-conditional content, repo scoping, shell safety, Edit-based docker-compose instructions
- tools.js wiring replaces the Phase 13 stub cleanly
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/15-job-prompt-completeness/15-01-SUMMARY.md`
</output>
