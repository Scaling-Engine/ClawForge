---
phase: 54-terminology-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chat/components/admin-instances-page.jsx
  - lib/chat/components/admin-layout.jsx
  - lib/chat/components/instance-switcher.jsx
  - lib/chat/components/superadmin-dashboard.jsx
  - lib/chat/components/superadmin-monitoring.jsx
  - lib/chat/components/superadmin-search.jsx
  - lib/chat/components/admin-billing-page.jsx
autonomous: true
requirements:
  - TERM-01

must_haves:
  truths:
    - "No logged-in user reads the word 'instance' or 'instances' in any heading, label, button, badge, or empty-state message"
    - "The admin sidebar nav item previously labeled 'Instances' now reads 'Agents'"
    - "The superadmin switcher dropdown label previously reading 'Instance' now reads 'Agent'"
    - "Superadmin dashboard heading and stat label updated from 'Instance(s)' to 'Agent(s)'"
    - "Monitoring dashboard heading and stat label updated from 'Instance(s)' to 'Agent(s)'"
    - "Job search table header column reads 'Agent' not 'Instance'"
    - "Billing page usage table row label reads 'Agent' not 'Instance'"
  artifacts:
    - path: "lib/chat/components/admin-layout.jsx"
      provides: "Admin sidebar nav — 'Instances' nav entry replaced with 'Agents'"
      contains: "label: 'Agents'"
    - path: "lib/chat/components/instance-switcher.jsx"
      provides: "Superadmin agent switcher dropdown label"
      contains: "Agent"
    - path: "lib/chat/components/admin-instances-page.jsx"
      provides: "Agents list page component with updated text"
      contains: "agent"
    - path: "lib/chat/components/superadmin-dashboard.jsx"
      provides: "Superadmin dashboard with updated headings"
      contains: "Cross-Agent Overview"
    - path: "lib/chat/components/superadmin-monitoring.jsx"
      provides: "Monitoring dashboard with updated headings"
      contains: "Agent Health Monitor"
    - path: "lib/chat/components/superadmin-search.jsx"
      provides: "Job search table with updated column header"
      contains: "Agent"
    - path: "lib/chat/components/admin-billing-page.jsx"
      provides: "Billing page with updated row label"
      contains: "Agent"
  key_links:
    - from: "lib/chat/components/admin-layout.jsx"
      to: "ADMIN_NAV array entry"
      via: "label property"
      pattern: "label: 'Agents'"
    - from: "lib/chat/components/instance-switcher.jsx"
      to: "dropdown label"
      via: "JSX text node"
      pattern: ">Agent<"
---

<objective>
Replace every user-visible occurrence of "instance" and "instances" in UI component source files with "agent" and "agents". This is a JSX text / label surgery — no logic changes, no backend changes, no DB or env var changes.

Purpose: Satisfy TERM-01. Users must never read "instance" in the running platform.
Output: 7 updated .jsx source files + rebuilt .js outputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key constraint from STATE.md v4.0 decisions:
- "Terminology layer 1 only: UI strings renamed to 'agent/agents' in v4.0. DB column names (instance_name), env var names, and directory names unchanged — breaking rename deferred post-v4.0."
- Only user-visible text in JSX/templates changes. JS variable names (e.g. `const instances = ...`, `instance.name`, `setInstances`) do NOT change.

Build note: `.js` files in `lib/chat/components/` are esbuild-compiled outputs of `.jsx` sources. Edit only `.jsx` files and run `npm run build` after to regenerate `.js` outputs.
</context>

<interfaces>
<!-- Key identifiers the executor needs — these are CODE identifiers, NOT text to change. -->

JS variable names that must NOT be changed:
- `instances` (state variable), `instance` (prop name), `setInstances`, `getInstancesOverview`, `getInstanceRegistryAction`, `getInstanceRegistryAction`, `InstanceCard`, `InstanceSwitcher`, `AdminInstancesPage`
- `clawforge-active-instance` (localStorage key)
- `inst` (loop variable alias)
- `instance.name`, `instance.status`, `instance.activeJobs`, etc. (object property access)

User-facing text that MUST change (exact current text → replacement):
In admin-layout.jsx:
  - `label: 'Instances'` → `label: 'Agents'`

In instance-switcher.jsx:
  - JSX text node `Instance` (inside `<label>`) → `Agent`

In admin-instances-page.jsx:
  - Template literal: `` `${instances.length} instance${instances.length !== 1 ? 's' : ''}` `` → `` `${instances.length} agent${instances.length !== 1 ? 's' : ''}` ``
  - `No instances found` → `No agents found`
  - `Instance directories were not detected. Running in single-instance mode.` → `Agent directories were not detected. Running in single-agent mode.`

In superadmin-dashboard.jsx:
  - `Cross-Instance Overview` → `Cross-Agent Overview`
  - `<div className="text-xs text-muted-foreground">Instances</div>` (the stat label) → `Agents`
  - `No instances configured. Set SUPERADMIN_INSTANCES environment variable.` → `No agents configured. Set SUPERADMIN_INSTANCES environment variable.`

In superadmin-monitoring.jsx:
  - `Instance Health Monitor` → `Agent Health Monitor`
  - `<div className="text-xs text-muted-foreground">Instances</div>` (the stat label) → `Agents`
  - `No instances configured.` → `No agents configured.`

In superadmin-search.jsx:
  - `<th ...>Instance</th>` (table column header) → `Agent`

In admin-billing-page.jsx:
  - `<span className="text-sm font-medium">Instance</span>` (usage table row label) → `Agent`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update admin-layout, instance-switcher, and admin-instances-page JSX text</name>
  <read_first>
    lib/chat/components/admin-layout.jsx
    lib/chat/components/instance-switcher.jsx
    lib/chat/components/admin-instances-page.jsx
  </read_first>
  <files>
    lib/chat/components/admin-layout.jsx,
    lib/chat/components/instance-switcher.jsx,
    lib/chat/components/admin-instances-page.jsx
  </files>
  <action>
    Make the following targeted text changes. Variable names, function names, href paths, and logic are unchanged.

    1. In `lib/chat/components/admin-layout.jsx`:
       - Line 17: change `label: 'Instances'` to `label: 'Agents'`
       - Leave `href: '/admin/instances'` unchanged (URL route change is TERM-02, handled in plan 02)
       - Leave `id: 'instances'` unchanged (code identifier)

    2. In `lib/chat/components/instance-switcher.jsx`:
       - Line 52: change the JSX text node `Instance` (inside the `<label>` element) to `Agent`
       - All other content unchanged — `instances` state variable, `activeInstance`, `setInstances`, `getInstanceRegistryAction`, `InstanceSwitcher` function name all stay as-is

    3. In `lib/chat/components/admin-instances-page.jsx`:
       - Line 87: in the template literal that displays count, change `instance` / `instances` text portions to `agent` / `agents`. The exact string is: `` `${instances.length} instance${instances.length !== 1 ? 's' : ''}` `` → `` `${instances.length} agent${instances.length !== 1 ? 's' : ''}` ``
       - Line 98: change JSX text `No instances found` to `No agents found`
       - Lines 99-100: change `Instance directories were not detected. Running in single-instance mode.` to `Agent directories were not detected. Running in single-agent mode.`
       - All JS identifiers (`InstanceCard`, `AdminInstancesPage`, `instances` variable, `instance` prop, etc.) unchanged
  </action>
  <verify>
    <automated>grep -n "label: 'Agents'" lib/chat/components/admin-layout.jsx && grep -n ">Agent<" lib/chat/components/instance-switcher.jsx && grep -n "No agents found" lib/chat/components/admin-instances-page.jsx && grep -n "single-agent mode" lib/chat/components/admin-instances-page.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "label: 'Agents'" lib/chat/components/admin-layout.jsx` returns a match
    - `grep "label: 'Instances'" lib/chat/components/admin-layout.jsx` returns NO match
    - `grep ">Agent<" lib/chat/components/instance-switcher.jsx` returns a match (the label text node)
    - `grep "No agents found" lib/chat/components/admin-instances-page.jsx` returns a match
    - `grep "No instances found" lib/chat/components/admin-instances-page.jsx` returns NO match
    - `grep "single-agent mode" lib/chat/components/admin-instances-page.jsx` returns a match
    - JS identifiers like `InstanceCard`, `AdminInstancesPage`, `instances` variable still present in the file (variable names unchanged)
  </acceptance_criteria>
  <done>All three files have updated user-facing text; no JS identifiers or function names changed.</done>
</task>

<task type="auto">
  <name>Task 2: Update superadmin-dashboard, superadmin-monitoring, superadmin-search, admin-billing-page, then rebuild</name>
  <read_first>
    lib/chat/components/superadmin-dashboard.jsx
    lib/chat/components/superadmin-monitoring.jsx
    lib/chat/components/superadmin-search.jsx
    lib/chat/components/admin-billing-page.jsx
  </read_first>
  <files>
    lib/chat/components/superadmin-dashboard.jsx,
    lib/chat/components/superadmin-monitoring.jsx,
    lib/chat/components/superadmin-search.jsx,
    lib/chat/components/admin-billing-page.jsx
  </files>
  <action>
    Make targeted JSX text changes only. Variable names, function names, and logic are unchanged.

    1. In `lib/chat/components/superadmin-dashboard.jsx`:
       - Line 139: change `Cross-Instance Overview` to `Cross-Agent Overview`
       - Line 144: change the stat label `Instances` (the text node inside `<div className="text-xs text-muted-foreground">`) to `Agents`
       - Line 168: change `No instances configured. Set SUPERADMIN_INSTANCES environment variable.` to `No agents configured. Set SUPERADMIN_INSTANCES environment variable.`
       - Note: `InstanceCard` function name, `instances` variable, `instance` prop names — all unchanged

    2. In `lib/chat/components/superadmin-monitoring.jsx`:
       - Line 195: change `Instance Health Monitor` to `Agent Health Monitor`
       - Line 199: change the stat label `Instances` (text node inside `<div className="text-xs text-muted-foreground">`) to `Agents`
       - Line 225: change `No instances configured.` to `No agents configured.`
       - Note: `MonitoringCard`, `instances` variable, `instance` prop — all unchanged

    3. In `lib/chat/components/superadmin-search.jsx`:
       - Find the `<th>` column header that reads `Instance` (approximately line 157) and change it to `Agent`
       - The key column, class names, and all JS logic unchanged

    4. In `lib/chat/components/admin-billing-page.jsx`:
       - Find the `<span className="text-sm font-medium">Instance</span>` label (approximately line 218) and change it to `Agent`
       - The `{instance}` value display on the next line and all JS identifiers unchanged

    After all file edits, run the esbuild compilation to regenerate .js outputs:
    ```
    npm run build
    ```
    This regenerates the `.js` compiled files from all updated `.jsx` sources.
  </action>
  <verify>
    <automated>grep -n "Cross-Agent Overview" lib/chat/components/superadmin-dashboard.jsx && grep -n "Agent Health Monitor" lib/chat/components/superadmin-monitoring.jsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Cross-Agent Overview" lib/chat/components/superadmin-dashboard.jsx` returns a match
    - `grep "Cross-Instance Overview" lib/chat/components/superadmin-dashboard.jsx` returns NO match
    - `grep "Agent Health Monitor" lib/chat/components/superadmin-monitoring.jsx` returns a match
    - `grep "Instance Health Monitor" lib/chat/components/superadmin-monitoring.jsx` returns NO match
    - `grep "No agents configured" lib/chat/components/superadmin-dashboard.jsx` returns a match
    - `grep "No agents configured" lib/chat/components/superadmin-monitoring.jsx` returns a match
    - `grep ">Agent<" lib/chat/components/superadmin-search.jsx` returns a match (the th column header)
    - `grep ">Instance<" lib/chat/components/superadmin-search.jsx` returns NO match
    - `grep "Agent.*text-sm font-medium\|text-sm font-medium.*Agent" lib/chat/components/admin-billing-page.jsx` OR `grep "Instance.*text-sm font-medium" lib/chat/components/admin-billing-page.jsx` returns NO match
    - `npm run build` exits 0 with no errors
    - Compiled `lib/chat/components/superadmin-dashboard.js` contains `Cross-Agent Overview`
    - Compiled `lib/chat/components/superadmin-monitoring.js` contains `Agent Health Monitor`
  </acceptance_criteria>
  <done>All 4 files updated, npm run build succeeds, compiled .js outputs contain the new text.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the final check:

```bash
# No user-facing "instance"/"instances" text remaining in .jsx sources
grep -rn '"Instance"\|"Instances"\|>Instance<\|>Instances<\|No instances\|single-instance\|instance${' lib/chat/components/*.jsx 2>/dev/null

# Confirm agent text present in key files
grep -l "Agents\|agents\|Agent" lib/chat/components/admin-layout.jsx lib/chat/components/instance-switcher.jsx lib/chat/components/superadmin-dashboard.jsx lib/chat/components/superadmin-monitoring.jsx
```

The first command should return NO matches (all user-facing instance text replaced).
The second command should return all 4 files.
</verification>

<success_criteria>
1. Every user-facing text occurrence of "Instance"/"Instances" in the 7 .jsx files has been replaced with "Agent"/"Agents"
2. No JS variable names, function names, prop names, or object property accesses were changed
3. `npm run build` exits 0 — compiled .js outputs are consistent with .jsx sources
4. A logged-in admin user navigating to the admin panel sees "Agents" in the sidebar (not "Instances"), the superadmin dropdown reads "Agent", and all dashboard headings use "agent" terminology
</success_criteria>

<output>
After completion, create `.planning/phases/54-terminology-migration/54-01-SUMMARY.md`
</output>
