---
phase: 51-code-mode-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chat/components/chat.jsx
  - lib/chat/features-context.js
  - lib/chat/components/code/actions.js
  - lib/db/workspaces.js
autonomous: true
gap_closure: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "useFeature('codeWorkspace') returns true when FeaturesProvider passes codeWorkspace:true"
    - "Code toggle and Interactive button render for admin users when codeWorkspace flag is true"
    - "Resume button only appears for workspaces with status 'running'"
    - "Clicking Resume for a running workspace navigates to /code/{id} without redirect loop"
  artifacts:
    - path: "lib/chat/components/chat.jsx"
      provides: "Fixed import path for features-context"
      contains: "features-context.jsx"
    - path: "lib/chat/components/code/actions.js"
      provides: "getLinkedWorkspace filtered to running only"
      contains: "running"
    - path: "lib/db/workspaces.js"
      provides: "getWorkspaceByChatId filtered to running only"
      contains: "running"
  key_links:
    - from: "lib/chat/components/chat.jsx"
      to: "lib/chat/features-context.jsx"
      via: "import useFeature"
      pattern: "from.*features-context\\.jsx"
    - from: "lib/chat/components/chat.jsx"
      to: "lib/chat/components/code/actions.js"
      via: "getLinkedWorkspace call on mount"
      pattern: "getLinkedWorkspace"
    - from: "lib/chat/components/code/actions.js"
      to: "lib/db/workspaces.js"
      via: "getWorkspaceByChatId query"
      pattern: "getWorkspaceByChatId"
---

<objective>
Fix two diagnosed bugs from the v1.0 milestone audit that break Code mode functionality.

Purpose: BUG-1 (split context) makes Code toggle and Interactive button permanently invisible. BUG-2 (resume starting) causes redirect loops when resuming non-running workspaces. Both are blocking Code mode usability.
Output: Two targeted fixes restoring Code mode toggle visibility and safe Resume behavior.
</objective>

<execution_context>
@/Users/nwessel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nwessel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

@lib/chat/features-context.jsx
@lib/chat/features-context.js
@lib/chat/components/chat.jsx
@lib/chat/components/code/actions.js
@lib/db/workspaces.js
@templates/app/code/[id]/page.js
</context>

<interfaces>
<!-- Key contracts the executor needs -->

From lib/chat/features-context.jsx (SOURCE — the correct module):
```jsx
export function FeaturesProvider({ flags = {}, children })
export function useFeature(flag) // returns Boolean(flags[flag])
```

From lib/chat/features-context.js (COMPILED DUPLICATE — must be deleted):
```js
// Separate createContext({}) call — Provider and consumer never share state
export { FeaturesProvider, useFeature }
```

From lib/chat/components/chat.jsx line 13 (BROKEN import):
```jsx
import { useFeature } from '../features-context.js';  // imports from WRONG module
```

From lib/chat/components/chat.jsx lines 82-84 (BROKEN workspace filter):
```jsx
getLinkedWorkspace({ chatId }).then(({ workspace }) => {
  if (workspace && (workspace.status === 'running' || workspace.status === 'starting')) {
    setLinkedWorkspaceId(workspace.id);
```

From lib/db/workspaces.js lines 150-157 (returns non-running workspaces):
```js
export function getWorkspaceByChatId(chatId) {
  // ...
  if (!ws || ws.status === 'destroyed') return undefined;  // allows 'starting', 'creating'
  return ws;
}
```

From templates/app/code/[id]/page.js lines 16-17 (only accepts running):
```js
if (!workspace || workspace.status !== 'running') {
  redirect('/chats');
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix split-module context (FIX-01)</name>
  <files>lib/chat/components/chat.jsx, lib/chat/features-context.js</files>
  <read_first>
    - lib/chat/components/chat.jsx (line 13 — the broken import)
    - lib/chat/features-context.js (the compiled duplicate to delete)
    - lib/chat/features-context.jsx (the source file — do NOT modify)
    - lib/chat/components/chat-page.jsx (line 9 — verify it already imports from .jsx)
  </read_first>
  <action>
1. In `lib/chat/components/chat.jsx` line 13, change:
   `import { useFeature } from '../features-context.js';`
   to:
   `import { useFeature } from '../features-context.jsx';`

2. Delete `lib/chat/features-context.js` (the compiled esbuild artifact that creates a separate React context). Run: `rm lib/chat/features-context.js`

3. Verify no other source files (.jsx) import from `features-context.js`. The compiled `.js` files in `lib/chat/components/` (chat.js, chat-page.js) are esbuild output and will be regenerated — do NOT modify them.

Why this works: `features-context.jsx` is the source with the Provider's createContext. `features-context.js` was a compiled copy with its own createContext. By pointing chat.jsx at the .jsx source, useFeature and FeaturesProvider share the same context object.
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && grep -n "features-context" lib/chat/components/chat.jsx | grep -v "\.jsx" && echo "FAIL: still imports .js" || echo "PASS: imports .jsx" && test ! -f lib/chat/features-context.js && echo "PASS: .js deleted" || echo "FAIL: .js still exists"</automated>
  </verify>
  <acceptance_criteria>
    - grep "features-context.jsx" lib/chat/components/chat.jsx returns a match
    - grep "features-context.js" lib/chat/components/chat.jsx returns NO match (excluding .jsx)
    - lib/chat/features-context.js does not exist on disk
    - lib/chat/features-context.jsx still exists unchanged
    - npm run build succeeds
  </acceptance_criteria>
  <done>chat.jsx imports useFeature from features-context.jsx (same module as FeaturesProvider), compiled duplicate deleted, useFeature('codeWorkspace') returns the actual flag value</done>
</task>

<task type="auto">
  <name>Task 2: Fix Resume button redirect loop (FIX-02)</name>
  <files>lib/chat/components/chat.jsx, lib/db/workspaces.js</files>
  <read_first>
    - lib/chat/components/chat.jsx (lines 80-90 — getLinkedWorkspace effect)
    - lib/db/workspaces.js (lines 150-157 — getWorkspaceByChatId function)
    - lib/chat/components/code/actions.js (lines 69-77 — getLinkedWorkspace Server Action)
    - templates/app/code/[id]/page.js (lines 16-17 — running-only gate)
  </read_first>
  <action>
Two-layer fix to prevent Resume from appearing for non-running workspaces:

1. In `lib/chat/components/chat.jsx`, narrow the status check in the getLinkedWorkspace effect (around line 83). Change:
   `if (workspace && (workspace.status === 'running' || workspace.status === 'starting')) {`
   to:
   `if (workspace && workspace.status === 'running') {`

   This ensures linkedWorkspaceId is only set for workspaces that page.js will accept.

2. In `lib/db/workspaces.js` function `getWorkspaceByChatId` (around line 155), tighten the filter to only return running workspaces. Change:
   `if (!ws || ws.status === 'destroyed') return undefined;`
   to:
   `if (!ws || ws.status !== 'running') return undefined;`

   This makes the DB layer consistent — getWorkspaceByChatId only returns workspaces that are actually usable. The `launchWorkspace` Server Action in actions.js (line 39) also calls getWorkspaceByChatId and reuses existing workspaces — tightening the filter means it will only reuse running workspaces, which is correct (starting/creating workspaces should not be reused, they should be waited on or a new one created).

Why both layers: The chat.jsx filter is the immediate fix (prevents Resume button). The DB-layer filter is defense-in-depth so no caller of getWorkspaceByChatId can accidentally surface a non-running workspace.
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && grep "starting" lib/chat/components/chat.jsx | grep -v "//\|console\|TODO" && echo "FAIL: starting still in filter" || echo "PASS: no starting in filter" && grep "status !== 'running'" lib/db/workspaces.js && echo "PASS: DB filter tightened" || echo "FAIL: DB filter not updated"</automated>
  </verify>
  <acceptance_criteria>
    - grep "starting" lib/chat/components/chat.jsx returns no matches in the workspace status check
    - grep "status !== 'running'" lib/db/workspaces.js returns a match in getWorkspaceByChatId
    - The launchWorkspace function in actions.js still checks running/starting/creating for reuse (line 39) — WAIT: this will break because getWorkspaceByChatId now only returns running. Verify launchWorkspace still works: it calls getWorkspaceByChatId, gets null for starting workspaces, and creates a new one. This is acceptable — starting workspaces are transient and a new container dispatch is safe.
    - npm run build succeeds
  </acceptance_criteria>
  <done>Resume button only shows for running workspaces. Clicking Resume navigates to /code/{id} successfully because page.js accepts running status. No redirect loop.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run build` succeeds with zero errors
2. No source file imports from `features-context.js` (only `.jsx`)
3. `getWorkspaceByChatId` only returns running workspaces
4. Chat.jsx only sets linkedWorkspaceId for running workspaces
</verification>

<success_criteria>
- FIX-01: useFeature and FeaturesProvider share the same React context — Code toggle renders when codeWorkspace flag is true and user is admin
- FIX-02: Resume button only appears for running workspaces — no redirect loop from page.js
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/51-code-mode-bug-fixes/51-01-SUMMARY.md`
</output>
