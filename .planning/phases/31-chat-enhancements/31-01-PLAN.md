---
phase: 31-chat-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chat/components/message.jsx
  - lib/chat/components/chat.jsx
  - lib/chat/api.js
  - instances/noah/config/EVENT_HANDLER.md
  - instances/strategyES/config/EVENT_HANDLER.md
  - package.json
  - package-lock.json
autonomous: false
requirements: [CHAT-01, CHAT-02, CHAT-03]

must_haves:
  truths:
    - "Code blocks in assistant messages render with syntax highlighting (Shiki) and have copy/collapse controls"
    - "When code mode toggle is active, the agent opens an interactive workspace instead of dispatching a headless job"
    - "File upload via drag-and-drop and paperclip button works for images, PDFs, and code files"
  artifacts:
    - path: "lib/chat/components/message.jsx"
      provides: "Streamdown code plugin integration with Shiki highlighting"
      contains: "@streamdown/code"
    - path: "lib/chat/components/chat.jsx"
      provides: "interactiveMode flag in transport body"
      contains: "interactiveMode"
    - path: "lib/chat/api.js"
      provides: "Server-side interactive mode hint injection"
      contains: "INTERACTIVE_MODE"
  key_links:
    - from: "lib/chat/components/chat.jsx"
      to: "lib/chat/api.js"
      via: "DefaultChatTransport body.interactiveMode"
      pattern: "interactiveMode.*codeMode"
    - from: "lib/chat/api.js"
      to: "lib/ai/index.js"
      via: "chatStream with [INTERACTIVE_MODE] prefix"
      pattern: "INTERACTIVE_MODE"
    - from: "lib/chat/components/message.jsx"
      to: "@streamdown/code"
      via: "plugins prop on Streamdown component"
      pattern: "plugins.*code"
---

<objective>
Bring the chat UI to feature parity with upstream PopeBot for message rendering and code mode routing.

Purpose: CHAT-03 adds syntax highlighting and collapsible code blocks to assistant messages via Streamdown's Shiki plugin. CHAT-02 extends the code mode toggle so it routes to interactive workspace creation instead of headless job dispatch. CHAT-01 is already fully implemented and is verified in the checkpoint.

Output: Enhanced message rendering with syntax-highlighted code blocks, interactive mode routing via transport body flag, and end-to-end verification of all three chat requirements.
</objective>

<execution_context>
@/Users/nwessel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nwessel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-chat-enhancements/31-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/chat/components/message.jsx (current Streamdown usage):
```jsx
import { Streamdown } from 'streamdown';

// Current rendering (no plugins, no controls):
<Streamdown mode={isLoading ? 'streaming' : 'static'} linkSafety={linkSafety}>{text}</Streamdown>
```

From lib/chat/components/chat.jsx (transport body):
```jsx
const transport = useMemo(
  () =>
    new DefaultChatTransport({
      api: '/stream/chat',
      body: {
        chatId,
        selectedRepo: selectedRepo?.slug || null,
        selectedBranch: selectedBranch || null,
      },
    }),
  [chatId, selectedRepo, selectedBranch]
);
```

From lib/chat/api.js (body destructuring):
```js
const { messages, chatId: rawChatId, trigger, selectedRepo, selectedBranch } = body;
```

From lib/chat/components/chat.jsx (codeMode state):
```jsx
const [codeMode, setCodeMode] = useState(false);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @streamdown/code and wire Shiki syntax highlighting into message rendering</name>
  <files>package.json, package-lock.json, lib/chat/components/message.jsx</files>
  <action>
1. Install the Streamdown code plugin:
   ```bash
   npm install @streamdown/code
   ```

2. In `lib/chat/components/message.jsx`:
   - Add import at top: `import { code } from '@streamdown/code';`
   - Create the plugin instance at MODULE LEVEL (outside any component, after imports): `const codePlugin = code();` — if `code` is not a factory (i.e., calling it throws), use `const codePlugin = code;` directly. Check the import's type to determine which.
   - In `renderTextWithStreamViewer()`, update ALL three `<Streamdown>` instances (lines ~88, ~98, ~102) to include:
     - `plugins={{ code: codePlugin }}`
     - `shikiTheme={['github-light', 'github-dark']}`
     - `controls={isLoading ? false : { code: true }}` — this prevents jitter during streaming. For the `mode="static"` instance (the `after` text), always pass `controls={{ code: true }}`.

   The three Streamdown instances are:
   - Line ~88: `<Streamdown mode={isLoading ? 'streaming' : 'static'} ...>` (no match case)
   - Line ~98: `<Streamdown mode={isLoading ? 'streaming' : 'static'} ...>` (before text)
   - Line ~102: `<Streamdown mode="static" ...>` (after text — always static, always controls)

3. Do NOT modify `globals.css` — it already has `@source "../node_modules/streamdown/dist/**/*.js"` which covers @streamdown/code's dist classes.

4. Do NOT install `shiki` separately — `@streamdown/code` bundles it as a direct dependency.
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && npm run build</automated>
  </verify>
  <done>All Streamdown instances in message.jsx use the code plugin with Shiki highlighting. Build passes. Code blocks in assistant messages will render with syntax highlighting, copy buttons, and collapse toggles.</done>
</task>

<task type="auto">
  <name>Task 2: Add interactive mode routing for code mode toggle</name>
  <files>lib/chat/components/chat.jsx, lib/chat/api.js, instances/noah/config/EVENT_HANDLER.md, instances/strategyES/config/EVENT_HANDLER.md</files>
  <action>
1. In `lib/chat/components/chat.jsx`:
   - Add `interactiveMode: codeMode` to the `body` object inside `DefaultChatTransport`:
     ```jsx
     body: {
       chatId,
       selectedRepo: selectedRepo?.slug || null,
       selectedBranch: selectedBranch || null,
       interactiveMode: codeMode,
     },
     ```
   - Add `codeMode` to the `useMemo` dependency array: `[chatId, selectedRepo, selectedBranch, codeMode]`

2. In `lib/chat/api.js`:
   - Add `interactiveMode` to the destructuring of `body` (line ~15):
     ```js
     const { messages, chatId: rawChatId, trigger, selectedRepo, selectedBranch, interactiveMode } = body;
     ```
   - After the `selectedRepo` context injection block (after line ~70), add:
     ```js
     // When code mode is active, signal the agent to use interactive workspace
     if (interactiveMode) {
       userText = `[INTERACTIVE_MODE: true]\n\n${userText}`;
     }
     ```
   - Place this AFTER the repo context injection so the hint appears at the very start of userText (repo context was prepended, then interactive mode prepends before that).
   - Actually, to ensure the hint is at the very top, place the interactive mode injection AFTER the repo injection. The final order will be: `[INTERACTIVE_MODE: true]\n\n[Active repo context: ...]\n\nuser text`. This is correct — the hint is the first thing the agent sees.

3. In `instances/noah/config/EVENT_HANDLER.md`:
   - Add a new section after the "## Not Everything is a Job" section (before "## Job Description Best Practices"):
     ```markdown
     ---

     ## Interactive Mode

     When a message begins with `[INTERACTIVE_MODE: true]`, the user has activated code mode in the web chat. Instead of dispatching a headless job via `create_job`, use the `start_coding` tool to open an interactive workspace where the user can collaborate with Claude Code in real-time.

     The rest of the message after the tag is the user's coding request. Pass the selected repo context (if present) to `start_coding`.

     ---
     ```

4. In `instances/strategyES/config/EVENT_HANDLER.md`:
   - Add the identical "## Interactive Mode" section in the same relative position. If the file structure differs, add it after any "Not Everything is a Job" equivalent, or at the end before any closing sections.
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && npm run build</automated>
  </verify>
  <done>Code mode toggle now sends `interactiveMode: true` in transport body. Server-side api.js prepends `[INTERACTIVE_MODE: true]` hint. Both instance EVENT_HANDLER.md files instruct the agent to route to `start_coding` when the hint is present. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all three chat enhancements end-to-end</name>
  <action>
Verify CHAT-03 (syntax highlighting), CHAT-02 (interactive mode routing), and CHAT-01 (file upload — already implemented, regression check).

1. Start dev server: `npm run dev`
2. Open web chat at http://localhost:3000/chat

**CHAT-03 — Syntax Highlighting:**
3. Send a message asking the agent to show a code example (e.g., "Show me a hello world in Python")
4. Verify the code block in the response has:
   - Syntax highlighting (colored tokens, not plain monospace)
   - A copy button on the code block
   - A collapse/expand toggle on the code block
5. Verify no flicker during streaming — controls should only appear after streaming completes

**CHAT-01 — File Upload:**
6. Click the paperclip button in chat input — verify file picker opens
7. Select an image file — verify thumbnail preview appears in the input area
8. Drag and drop a text file onto the chat area — verify file chip appears
9. Send the message with attachments — verify files are included in the message

**CHAT-02 — Interactive Mode:**
10. Toggle the code mode button (&lt;/&gt; icon) in chat input — it should highlight
11. Send a coding request (e.g., "Fix the login page")
12. Verify the agent invokes `start_coding` (workspace creation) instead of `create_job` (headless dispatch)
13. Toggle code mode off and send a similar request — verify it dispatches a headless job via `create_job`
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && npm run build</automated>
  </verify>
  <done>All three chat requirements verified: syntax highlighting renders with Shiki, code mode routes to interactive workspace, file upload works end-to-end.</done>
</task>

</tasks>

<verification>
- `npm run build` passes after each task
- All three Streamdown instances in message.jsx use the code plugin
- Transport body includes interactiveMode flag keyed to codeMode state
- api.js prepends [INTERACTIVE_MODE: true] when flag is set
- Both instance EVENT_HANDLER.md files contain interactive mode routing instruction
- File upload (CHAT-01) continues working as before (no regression)
</verification>

<success_criteria>
1. Code blocks in assistant messages render with Shiki syntax highlighting, copy buttons, and collapsible controls
2. Code mode toggle routes messages to interactive workspace (start_coding) instead of headless job dispatch (create_job)
3. File upload via drag-and-drop and paperclip button works end-to-end (existing feature, no regression)
4. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-chat-enhancements/31-01-SUMMARY.md`
</output>
