---
phase: 47-commercial-launch-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SLACK_NOTIFICATION_AUDIT.md
  - lib/ai/tools.js
autonomous: true
requirements:
  - LAUNCH-01

must_haves:
  truths:
    - "Every Slack chat.postMessage and chat.update call in the codebase is catalogued with its file, line, message format, and purpose"
    - "No Slack notification format has changed between pre-v3.0 and current code -- only additive fields (new call sites) were introduced"
    - "The audit document lists which notifications are pre-v3.0 (must not change) and which are new v3.0 additions"
  artifacts:
    - path: "docs/SLACK_NOTIFICATION_AUDIT.md"
      provides: "Complete audit of all Slack notification call sites with format documentation"
      contains: "AUDIT RESULT"
  key_links:
    - from: "docs/SLACK_NOTIFICATION_AUDIT.md"
      to: "lib/ai/tools.js"
      via: "documents every chat.postMessage call"
      pattern: "chat\\.postMessage"
---

<objective>
Audit every Slack notification call site in the codebase and confirm zero breaking changes to existing message formats before opening external customer access.

Purpose: Protect Noah and StrategyES operators from notification regressions -- their Slack search queries and automations depend on stable message structure.
Output: docs/SLACK_NOTIFICATION_AUDIT.md documenting every call site, message format, and pre-v3.0 vs new-in-v3.0 classification.
</objective>

<execution_context>
@/Users/nwessel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nwessel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- All Slack notification call sites identified during planning (executor should verify completeness): -->

1. lib/ai/tools.js:102-105 — 80% billing warning to SLACK_OPERATOR_CHANNEL (NEW in v3.0, Phase 44)
2. lib/ai/tools.js:220-224 — Job started status message in thread (pre-v3.0, Phase 39 STRM-06)
3. lib/ai/tools.js:244-248 — Job running status edit-in-place update (pre-v3.0, Phase 39 STRM-06)
4. lib/ai/tools.js:397-401 — Job completion: edit status message to final result (pre-v3.0)
5. lib/ai/tools.js:403 — Job completion: full summary posted to thread (pre-v3.0)
6. lib/ai/tools.js:406 — Job completion: fallback non-streaming post (pre-v3.0)
7. api/index.js:332 — GitHub Actions webhook: job result to thread (pre-v3.0)
8. lib/tools/docker.js:1128 — Workspace event notification to thread (pre-v3.0, Phase 22)
9. lib/monitoring/alerts.js:34-36 — Consecutive failure alert to SLACK_OPERATOR_CHANNEL (NEW in v3.0, Phase 46)
10. lib/cluster/index.js:61 — Cluster run started parent message (pre-v3.0, Phase 28)
11. lib/cluster/index.js:106 — Cluster run completion message (pre-v3.0, Phase 28)
12. lib/cluster/index.js:133 — Cluster run failure message (pre-v3.0, Phase 28)
13. lib/cluster/coordinator.js:351 — Per-agent completion in cluster thread (pre-v3.0, Phase 28)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit all Slack notification call sites and produce audit document</name>
  <read_first>
    - lib/ai/tools.js (lines 85-470 — job dispatch, waitAndNotify, billing warning)
    - api/index.js (lines 250-340 — handleGithubWebhook notification path)
    - lib/tools/docker.js (lines 1079-1170 — notifyWorkspaceEvent)
    - lib/monitoring/alerts.js (entire file — consecutive failure alert)
    - lib/cluster/index.js (lines 50-143 — cluster Slack messages)
    - lib/cluster/coordinator.js (lines 340-360 — per-agent Slack notify)
    - lib/channels/slack.js (entire file — SlackAdapter.sendResponse for completeness)
  </read_first>
  <files>docs/SLACK_NOTIFICATION_AUDIT.md</files>
  <action>
    Search the entire codebase for every `chat.postMessage`, `chat.update`, and `sendResponse` call that sends to Slack. For each call site, document:

    1. File path and line number
    2. The exact message text template (showing interpolated variables)
    3. Whether the call uses plain text or Block Kit
    4. The destination (thread reply vs. channel post vs. SLACK_OPERATOR_CHANNEL)
    5. Classification: "PRE-V3.0" (existed before Phase 43) or "NEW-V3.0" (added in Phases 43-46)

    Create `docs/SLACK_NOTIFICATION_AUDIT.md` with this structure:

    ```markdown
    # Slack Notification Audit — v3.0 Launch Readiness

    **Audited:** {date}
    **AUDIT RESULT:** PASS — No pre-v3.0 notification formats were modified

    ## Summary

    - {N} total Slack notification call sites
    - {X} pre-v3.0 (unchanged, format preserved)
    - {Y} new in v3.0 (additive only, no impact on existing automations)

    ## Pre-v3.0 Notifications (Must Not Change)

    ### Job Notifications (lib/ai/tools.js)
    | Call Site | Format | Destination |
    ...

    ### Workspace Notifications (lib/tools/docker.js)
    ...

    ### Cluster Notifications (lib/cluster/)
    ...

    ### GitHub Actions Webhook (api/index.js)
    ...

    ## New v3.0 Notifications (Additive Only)

    ### Billing Warning (lib/ai/tools.js:102)
    - Format: `Warning: Instance "{name}" has {current}/{limit} jobs...`
    - Destination: SLACK_OPERATOR_CHANNEL (new env var, not routed to user threads)
    ...

    ### Consecutive Failure Alert (lib/monitoring/alerts.js:34)
    ...

    ## Verification

    - [ ] Every `chat.postMessage` call in lib/ and api/ is listed above
    - [ ] No pre-v3.0 message format string was modified by Phases 43-46
    - [ ] New v3.0 notifications go to SLACK_OPERATOR_CHANNEL only (not user threads)
    - [ ] SlackAdapter.sendResponse (conversational replies) is unchanged
    ```

    To verify classifications, use `git log --oneline --all -- {file}` on each file to check when notification code was last modified. Pre-v3.0 means no changes to the message format string since before Phase 43 commits.

    The audit MUST confirm that:
    - Job completion messages (summarizeJob output) format is identical to pre-v3.0
    - Workspace event messages use the same switch/case text as before
    - Cluster messages use the same template strings as before
    - The only NEW Slack messages are the billing 80% warning and consecutive failure alert, both routed to SLACK_OPERATOR_CHANNEL (not user conversation threads)
  </action>
  <acceptance_criteria>
    - grep -q "AUDIT RESULT" docs/SLACK_NOTIFICATION_AUDIT.md
    - grep -q "PRE-V3.0\|pre-v3.0\|Pre-v3.0" docs/SLACK_NOTIFICATION_AUDIT.md
    - grep -q "NEW.*V3.0\|new.*v3.0\|New.*v3.0\|Additive" docs/SLACK_NOTIFICATION_AUDIT.md
    - grep -q "chat.postMessage\|postMessage" docs/SLACK_NOTIFICATION_AUDIT.md
    - grep -q "SLACK_OPERATOR_CHANNEL" docs/SLACK_NOTIFICATION_AUDIT.md
    - grep -c "lib/ai/tools.js" docs/SLACK_NOTIFICATION_AUDIT.md returns at least 3
    - grep -c "lib/cluster" docs/SLACK_NOTIFICATION_AUDIT.md returns at least 2
  </acceptance_criteria>
  <verify>
    <automated>grep -c "chat.postMessage\|chat.update\|sendResponse" docs/SLACK_NOTIFICATION_AUDIT.md | xargs test 8 -le</automated>
  </verify>
  <done>Every Slack notification call site is catalogued with format, destination, and pre/post-v3.0 classification. Audit document confirms zero format changes to pre-v3.0 notifications.</done>
</task>

</tasks>

<verification>
- docs/SLACK_NOTIFICATION_AUDIT.md exists and contains a PASS/FAIL audit result
- Every chat.postMessage and chat.update call in lib/ and api/ is accounted for
- Pre-v3.0 notifications are confirmed unchanged
- New v3.0 notifications are documented as additive-only
</verification>

<success_criteria>
The audit document proves that no existing Slack notification format was modified by v3.0 work, and all new notifications are additive (routed to SLACK_OPERATOR_CHANNEL, not user threads).
</success_criteria>

<output>
After completion, create `.planning/phases/47-commercial-launch-hardening/47-01-SUMMARY.md`
</output>
