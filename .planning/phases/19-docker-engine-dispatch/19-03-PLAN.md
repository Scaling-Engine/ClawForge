---
phase: 19-docker-engine-dispatch
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - docker-compose.yml
  - .env.example
autonomous: false

requirements: [DISP-04]

must_haves:
  truths:
    - "Event Handler containers have Docker socket mounted for sibling container dispatch"
    - "INSTANCE_NAME, DOCKER_NETWORK, JOB_IMAGE, and AGENT secret env vars are configured in docker-compose"
    - "Docker-dispatched jobs produce identical outputs to Actions-dispatched jobs"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Docker socket mount and new env vars for both instances"
      contains: "/var/run/docker.sock"
    - path: ".env.example"
      provides: "Documentation of new env vars"
      contains: "DOCKER_NETWORK"
  key_links:
    - from: "docker-compose.yml"
      to: "lib/tools/docker.js"
      via: "Docker socket volume mount enables dockerode connection"
      pattern: "docker\\.sock"
    - from: "docker-compose.yml"
      to: "lib/ai/tools.js"
      via: "INSTANCE_NAME, DOCKER_NETWORK, JOB_IMAGE env vars used in dispatch"
      pattern: "INSTANCE_NAME"
---

<objective>
Wire Docker socket access and new environment variables into docker-compose.yml so the Event Handler can dispatch sibling containers. Verify end-to-end functionality.

Purpose: Without the socket mount and env vars, the Docker client from Plans 01-02 can't connect to the daemon. This plan completes the integration.
Output: Updated docker-compose.yml and .env.example. E2E verification checkpoint.
</objective>

<execution_context>
@/Users/nwessel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nwessel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-docker-engine-dispatch/19-RESEARCH.md
@.planning/phases/19-docker-engine-dispatch/19-01-SUMMARY.md
@.planning/phases/19-docker-engine-dispatch/19-02-SUMMARY.md

@docker-compose.yml
@.env.example

<interfaces>
<!-- From Plan 01-02: env vars consumed by docker.js and tools.js -->
Environment variables used in code:
- process.env.JOB_IMAGE — Docker image for job containers (default: 'scalingengine/clawforge:job-latest')
- process.env.DOCKER_NETWORK — Docker network for job containers (default: 'noah-net')
- process.env.INSTANCE_NAME — Instance identifier for container labels (default: 'noah')
- process.env.AGENT_SECRETS — JSON string of AGENT_* secrets passed to job containers
- process.env.AGENT_LLM_SECRETS — JSON string of AGENT_LLM_* secrets passed to job containers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker Compose and env configuration</name>
  <files>docker-compose.yml, .env.example</files>
  <action>
1. Update `docker-compose.yml` noah-event-handler service:
   - Add Docker socket volume mount (read-only):
     ```yaml
     volumes:
       - /var/run/docker.sock:/var/run/docker.sock:ro
       - noah-data:/app/data
       - noah-config:/app/config
     ```
   - Add new environment variables:
     ```yaml
     INSTANCE_NAME: noah
     DOCKER_NETWORK: ${NOAH_DOCKER_NETWORK:-noah-net}
     JOB_IMAGE: ${NOAH_JOB_IMAGE:-scalingengine/clawforge:job-latest}
     AGENT_SECRETS: ${NOAH_AGENT_SECRETS:-{}}
     AGENT_LLM_SECRETS: ${NOAH_AGENT_LLM_SECRETS:-{}}
     ```

2. Update `docker-compose.yml` ses-event-handler service:
   - Add Docker socket volume mount (read-only):
     ```yaml
     volumes:
       - /var/run/docker.sock:/var/run/docker.sock:ro
       - ses-data:/app/data
       - ses-config:/app/config
     ```
   - Add new environment variables:
     ```yaml
     INSTANCE_NAME: strategyES
     DOCKER_NETWORK: ${SES_DOCKER_NETWORK:-strategyES-net}
     JOB_IMAGE: ${SES_JOB_IMAGE:-scalingengine/clawforge:job-latest}
     AGENT_SECRETS: ${SES_AGENT_SECRETS:-{}}
     AGENT_LLM_SECRETS: ${SES_AGENT_LLM_SECRETS:-{}}
     ```

3. Update `.env.example`:
   - Add new variables under each instance section with comments:
     ```
     # Docker Engine Dispatch (Phase 19)
     # Network name for job containers — must match docker-compose network name
     # Docker Compose prefixes with project name, e.g., clawforge_noah-net
     # Discover actual name: docker network ls | grep noah-net
     NOAH_DOCKER_NETWORK=noah-net
     NOAH_JOB_IMAGE=scalingengine/clawforge:job-latest
     # JSON objects of secrets to pass to job containers
     # AGENT_SECRETS: non-LLM secrets (GH_TOKEN, etc.)
     # AGENT_LLM_SECRETS: LLM API keys (visible to Claude Code)
     NOAH_AGENT_SECRETS={"AGENT_GH_TOKEN":"ghp_..."}
     NOAH_AGENT_LLM_SECRETS={"AGENT_LLM_ANTHROPIC_API_KEY":"sk-ant-..."}
     ```
   - Add equivalent SES_ prefixed variables.

4. IMPORTANT per Research Pitfall 3: The Docker Compose network name may be prefixed with the project name (e.g., `clawforge_noah-net`). The `DOCKER_NETWORK` env var should match the actual Docker network name. Add a comment in .env.example explaining this. The `initDocker()` function in docker.js should ideally discover the network at startup, but for now the env var override is sufficient.
  </action>
  <verify>
    <automated>cd "/Users/nwessel/Claude Code/Business/Products/clawforge" && grep -q "docker.sock" docker-compose.yml && grep -q "INSTANCE_NAME" docker-compose.yml && grep -q "DOCKER_NETWORK" docker-compose.yml && echo "PASS: docker-compose.yml updated" || echo "FAIL"</automated>
  </verify>
  <done>
    - docker-compose.yml mounts /var/run/docker.sock:ro on both event handler services
    - INSTANCE_NAME, DOCKER_NETWORK, JOB_IMAGE, AGENT_SECRETS, AGENT_LLM_SECRETS configured for both instances
    - .env.example documents all new variables with comments
    - Network naming caveat documented
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Docker dispatch verification</name>
  <action>
Verify the complete Docker Engine Dispatch system built across Plans 01-03:
- Plan 01: Docker client (dockerode), container lifecycle (create/start/wait/logs/remove/inspect), orphan reconciliation, DB tracking
- Plan 02: Dual-path dispatch routing (Docker vs Actions via REPOS.json), inline notification after container.wait(), webhook dedup
- Plan 03: Docker Compose wiring (socket mount, env vars)

Steps:
1. Code review: Check lib/tools/docker.js, lib/db/docker-jobs.js, lib/ai/tools.js, api/index.js, docker-compose.yml
2. Local validation: Run `node -e "import('./lib/tools/docker.js').then(m => m.initDocker().catch(e => console.log('Expected: no Docker socket locally', e.message)))"` -- should show connection error (expected without Docker)
3. Deploy test (on VPS with Docker):
   a. `docker compose up -d --build` to rebuild with socket mount
   b. Send a test job via Slack: "create a job that adds a comment to README.md"
   c. Verify container starts in ~15s (not ~60s), PR created, notification arrives in Slack thread
   d. Check `docker ps` during execution -- should see clawforge-job-* container
   e. After completion, `docker ps -a | grep clawforge-job` should show no leftover containers
4. Dedup test: If Actions workflow also fires notify-pr-complete.yml, verify only ONE notification appears in Slack thread
5. Fallback test: Temporarily set "dispatch": "actions" in REPOS.json, send another job, verify it goes through GitHub Actions as before
  </action>
  <verify>Operator confirms end-to-end Docker dispatch works</verify>
  <done>Docker-dispatched jobs start fast, produce correct PRs and notifications, clean up containers, and dedup against Actions webhooks</done>
</task>

</tasks>

<verification>
- docker-compose.yml has socket mount and new env vars for both instances
- .env.example documents all new variables
- End-to-end: Slack message -> Docker dispatch -> container runs -> PR created -> notification in Slack
- Dedup: Only one notification per Docker-dispatched job
- Fallback: Actions dispatch still works when configured
</verification>

<success_criteria>
Docker-dispatched jobs start in ~15 seconds, produce PRs and notifications identical to Actions-dispatched jobs, and clean up after themselves. The Actions fallback path remains fully functional.
</success_criteria>

<output>
After completion, create `.planning/phases/19-docker-engine-dispatch/19-03-SUMMARY.md`
</output>
